Add .env file and copy from .env.example content

Run this command in terminal:

composer install && php artisan key:generate && php artisan config:cache && npm install && npm run build && php artisan serve
